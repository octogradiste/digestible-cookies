export default interface CookieLevel {
  /** The title of the cookie level. */
  title: string;

  /** The required flag of the cookie level. */
  required: boolean;

  /** The description of the cookie level. */
  description: string;
}
